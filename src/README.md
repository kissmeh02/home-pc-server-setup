# Source (`src/`)

- **`parts/`** — ordered HTML fragments for the static infrastructure reference (see `parts/README.md`).

Presentation is split across parts; **do not** duplicate `<html>` or `<head>` outside `part01.html` / `part03.html` boundaries.
